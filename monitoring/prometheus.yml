global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts/*.yml"

scrape_configs:
  - job_name: "medical-platform"
    static_configs:
      - targets: ["app:3000"]
    metrics_path: /api/metrics
